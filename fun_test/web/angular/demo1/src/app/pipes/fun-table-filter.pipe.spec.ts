import { FunTableFilterPipe } from './fun-table-filter.pipe';

describe('FunTableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FunTableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
