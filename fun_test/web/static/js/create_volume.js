(function (angular) {
    'use strict';

    function CreateVolumeController($scope, $element) {
    }

    angular.module('tools').component('createVolume', {
        templateUrl: '/static/create_volume.html',
        controller: CreateVolumeController
    });
})(window.angular);
